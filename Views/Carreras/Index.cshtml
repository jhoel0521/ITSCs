@model IEnumerable<ITSC.Models.Carrera>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreC)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreC)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idc">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idc">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idc">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
